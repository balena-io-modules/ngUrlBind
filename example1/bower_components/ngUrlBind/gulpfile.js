//the build instructions are in gulpfile.coffee
//this file is a simple bridge since gulp doesn't support
//coffeescript files without an ungodly command line arg

require("coffee-script")
require("./gulpfile.coffee")